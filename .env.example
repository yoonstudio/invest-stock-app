# Investment MCP Server Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (comma-separated origins)
CORS_ORIGIN=http://localhost:3001,http://localhost:3000

# Logging
LOG_LEVEL=INFO

# External API Keys (Optional - uses mock data if not provided)
# News API from https://newsapi.org/
NEWS_API_KEY=

# Yahoo Finance API (No key required for basic usage)

# Exchange Rate API (exchangerate.host - No key required for basic usage)
